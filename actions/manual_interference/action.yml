name: 'Manual Interference'
description: 'fake manual interference action'
inputs:
  reason: 
    description: 'Reason to require manual interference'
    required: true
outputs:
  time:
    description: 'The time it uses'
  status: 
    description: 'Status of result'
runs:
  using: 'node12'
  main: 'manual_interference'
